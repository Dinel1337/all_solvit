
#user config
AUTH_PREFIX = '/auth'
AUTH_TAGS = ['auth']

USERS_PREFIX = '/users'
USERS_TAGS   = ['users']

QUIZ_PREFIX = '/quizzes'
QUIZ_TAGS   = ['quizzes']

ATTEMPTS_PREFIX = '/attempts'
ATTEMPTS_TAGS   = ['attempts']
OpenAPI_text = """
**Квиз-платформа: создание викторин, публикация по ссылке, прохождение с сохранением ответов и расчёт результатов.**

**Все запросы к созданию и редактированию квизов защищены JWT-токенами.**

Для полноценного тестирования необходимо:

1. **Зарегистрироваться** (через эндпойнт **`/auth/register`**).
2. **Получить JWT-токен** (через **`/auth/login`**).
3. **Нажать кнопку** **`Authorize`** в правом верхнем углу Swagger UI и вставить токен в формате: **`Bearer <your_access_token>`**.

**О проекте:**

Автор квиза может создавать викторины с вопросами и вариантами ответов, публиковать их и отправлять ссылку друзьям. Пользователи проходят квиз, система сохраняет ответы и автоматически рассчитывает результат с разбором по каждому вопросу.

**Основные разделы API:**

- **auth** — регистрация, вход, выход
- **users** — профиль пользователя
- **quizzes** — создание, публикация, получение квизов (пагинация, поиск)
- **attempts** — прохождение квиза, сохранение ответов, завершение, получение результата

**Технические детали:**

- Реляционная база данных (SQLite/PostgreSQL)
- REST API
- JWT-аутентификация
- RaiseControl — автоматическая генерация 404
- BaseRepository — generic CRUD
- ResponseData — фабрика Swagger-ответов

*Проект **расширен** от базовой задачи и приближен к реальному продакшн-бэкенду.*
"""