"""E2E тесты для модуля Quizzes (Викторины).

Покрытие:
- POST /quizzes/ — создание викторины с вопросами и опциями, валидация полей.
- GET /quizzes/ — получение списка викторин (только published), поиск по name/description, пагинация.
- GET /quizzes/{id} — получение викторины по ID (published и draft доступны владельцу).
- PUT /quizzes/{id}/publish — изменение статуса викторины (draft/published).

Особенности:
- Проверка бизнес-логики: ровно 1 правильный ответ на вопрос.
- Черновики не отображаются в публичном списке.
- Валидация query-параметров (limit, offset, search) и path-параметров (id).

Все тесты используют фикстуры с транзакционным откатом БД.
"""