"""E2E тесты для модуля Reports (Отчёты).

Покрытие:
- GET /reports/summary — сводка по тренировкам за период (workouts_done, total_sets, total_reps, total_volume).
- GET /reports/daily — дневная динамика прогресса (date, volume, workouts).
- Валидация query-параметров: from_date, to_date (обязательные, формат YYYY-MM-DD).
- Проверка пустого ответа при отсутствии тренировок в периоде.

Особенности:
- Все тесты используют фикстуры с предсозданными тренировками.
- Проверка корректности агрегаций (суммирование подходов, повторений, объёма).
"""